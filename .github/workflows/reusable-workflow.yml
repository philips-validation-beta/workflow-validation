name: Reusable workflow example

on:
  workflow_call:
    inputs:
      filename:
        required: true
        type: string


jobs:
  cat-file:
    runs-on: ubuntu-latest
    steps:
    - id: print
      run: | 
         cat ${{ inputs.filename }}
